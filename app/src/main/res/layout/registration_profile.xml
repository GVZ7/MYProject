<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditText
        android:id="@+id/username_registration"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="209dp"
        android:hint="Username"
        android:layout_alignParentLeft="true" />

    <!--com.facebook.drawee.view.SimpleDraweeView
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/pic_registration"
        app:placeholderImage="@drawable/ic_profile"
        android:onClick="choose_picture"/-->
    <Button
        android:id="@+id/registor"
        android:layout_width="100dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="49dp"
        android:background="@drawable/rounded_button"
        android:onClick="register"
        android:text="Register" />

    <!--TextView
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_alignBottom="@+id/pic_registration"
        android:layout_alignEnd="@+id/pic_registration"
        android:text="Choose Picture"
        android:layout_alignRight="@+id/pic_registration"
        android:textSize="21dp"
        android:paddingHorizontal="12dp"/-->


</RelativeLayout>